<h1><%= @patient.name %></h1>
<h2><%= @patient.age %></h2>

<h3>appointments</h3>

<% @patient.appointments.each do |a| %>  
  <ul>
    <li>
      <%= link_to "#{a.doctor.name} - #{a.appointment_datetime.strftime("%B %d, %Y at %H:%M")}", doctor_path(a.doctor) %>
    </li>
  </ul>
<% end %>